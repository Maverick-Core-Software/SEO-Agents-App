# crewai 1.15.0+ requires opentelemetry-*~=1.42.0. Earlier (1.14.x) pinned
# ~=1.34.0, whose 1.34.x series raises ImportError: cannot import name
# '_ON_EMIT_RECURSION_COUNT_KEY' on import. Staying on 1.15.x keeps the working
# opentelemetry as the resolver-native choice — no manual override needed.
crewai==1.15.1
crewai-tools==1.15.1
python-dotenv==1.2.2
